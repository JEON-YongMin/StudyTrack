spring:
  application:
    name: StudyTrack

  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb  # 주소를 testdb로 고정
    driver-class-name: org.h2.Driver
    username: sa
    password:

jwt:
  secret: "studytrack_access_token_secret_key_very_long_string_123456"
  access-token-exp-min: 60